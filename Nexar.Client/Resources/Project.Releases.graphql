query ProjectReleases($projectId: ID!) {
  desProjectById(id: $projectId) {
    design {
      releases(first: 1000) {
        nodes {
          ...MyRelease
        }
      }
    }
  }
}

fragment MyRelease on DesRelease {
  id
  releaseId
  description
  createdAt
}
