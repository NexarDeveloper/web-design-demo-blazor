query TeamUsers($workspaceUrl: String!) {
  desTeam(workspaceUrl: $workspaceUrl) {
    users {
      ...MyUser
    }
  }
}

fragment MyUser on DesUser {
  userId
  userName
  email
  firstName
  lastName
  pictureUrl
  groups {
    name
  }
}
