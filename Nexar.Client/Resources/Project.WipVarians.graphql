query ProjectWipVariants($id: ID!) {
  desProjectById(id: $id) {
    design {
      workInProgress {
        variants {
          ...MyWipVariant
        }
      }
    }
  }
}

fragment MyWipVariant on DesWipVariant {
  name
  bom {
    id
  }
}
