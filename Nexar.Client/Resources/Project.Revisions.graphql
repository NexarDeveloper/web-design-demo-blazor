query ProjectRevisions($id: ID!) {
  desProjectById(id: $id) {
    revisions(first: 10000) {
      nodes {
        ...MyRevision
      }
    }
  }
}

fragment MyRevision on DesVcsRevision {
  revisionId
  createdAt
  author
  message
}
